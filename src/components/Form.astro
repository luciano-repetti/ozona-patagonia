<div class="section-contact scrollspy" id="contacto">
  <section class="contact-form">
    <h2>Contáctanos</h2>
    <h3><span>Estamos aquí para responder tus </span>preguntas</h3>
    <!-- Asegúrate de que el atributo action apunte al script PHP -->
    <form action="/enviar_mensaje.php" method="POST">
      <div class="form-group">
        <label for="nombre">Nombre</label>
        <input type="text" id="nombre" name="nombre" required />
      </div>
      <div class="form-group">
        <label for="email">Correo electrónico</label>
        <input type="email" id="email" name="email" required />
      </div>
      <div class="form-group">
        <label for="mensaje">Mensaje</label>
        <textarea id="mensaje" name="mensaje" required></textarea>
      </div>
      <button type="submit" class="submit-btn">Enviar mensaje</button>
    </form>
  </section>
</div>

<style>
  .section-contact {
    width: 100%;
    min-height: 75svh;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: #f3f4f6;
    padding: 1em 0;
    /* padding: 5em 1em; */
  }

  .contact-form {
    margin: 0 auto;
    background-color: white;
    padding: 2rem;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    width: 100%;
    height: 100%;
    max-width: 400px;
  }

  .contact-form h2 {
    color: #1f2937;
    text-align: center;
    margin-bottom: 1rem;
  }

  .contact-form h3 {
    color: #6b7280;
    text-align: center;
    margin-bottom: 2rem;
    font-size: 0.875rem;
  }

  .form-group {
    margin-bottom: 1rem;
  }

  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: #4b5563;
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: 0.5rem;
    border: 1px solid #d1d5db;
    border-radius: 4px;
    font-size: 1rem;
  }

  .form-group textarea {
    resize: vertical;
    min-height: 100px;
    max-height: 200px;
  }

  .submit-btn {
    background-color: #dc2626;
    color: white;
    border: none;
    padding: 0.75rem 1rem;
    font-size: 1rem;
    border-radius: 4px;
    cursor: pointer;
    width: 100%;
    transition: background-color 0.3s ease;
  }

  .submit-btn:hover {
    background-color: #b91c1c;
  }

  @media (max-width: 480px) {
    .section-contact {
      height: auto;
      padding: 2em 1em;
    }

    .contact-form {
      padding: 1.5rem;
    }
  }
</style>
